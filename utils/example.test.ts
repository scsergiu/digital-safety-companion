/**
 * Sample unit test - no app/Expo imports to avoid scope issues in Jest.
 * Replace with real tests (e.g. utilities, pure functions) as needed.
 */
describe('sample', () => {
  it('passes a simple assertion', () => {
    expect(1 + 1).toBe(2);
  });
});
